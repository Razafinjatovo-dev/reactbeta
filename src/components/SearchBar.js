const SearchBar = () => {
    return (
        <div>
            <input type='text' placeholder="Search..." />
            <br/>
            <input type='checkbox' />
            <label>Only show products in stock</label>
        </div>
    )
}

export default SearchBar